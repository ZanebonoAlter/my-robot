/**
 * Preferences feature — public facade
 *
 * Cross-feature consumers should import stable preference tracking Interfaces from here.
 */

export { useReadingTracker, useScrollDepthTracker } from './composables/useReadingTracker'
