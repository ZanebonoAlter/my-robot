/**
 * TopicGraph feature — public facade
 *
 * 跨 feature 共享的稳定 Interface。
 * 其他 feature 如需使用 topic-graph 的组件，必须通过此文件导入，
 * 不得直接深 import features/topic-graph/components/ 内部。
 */

// TagMergePreview 已搬迁至 features/tags/；此 facade 现为空，随 topic-graph 目录在 Task 4 一并删除。
