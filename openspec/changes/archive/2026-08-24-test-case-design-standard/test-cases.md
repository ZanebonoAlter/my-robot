# test-cases: test-case-design-standard

> 首个按自身标准编写的用例文档（吃狗粮闭环）。本 change 含行为组件（spec-gate 检查⑤、test-assets.sh、摘要注入），不按「纯文档/工具链」豁免，出完整用例文档。
> 归档预期：⑤b 会对 tasks.md 中描述规则本身的 4 行字面 meta 提及（1.4 / 2.1 / 2.3 / T.2，均含「纯函数」与「SQLite」字样）各出 1 条 warn——非违例，可忽略；⑤a 因本文件存在而被检出抑制（实测验证，见 V.9）。

## 故事 S1: 我要查「测什么 / 测到什么程度」的标准（锚 Requirement: 用例设计标准权威源）

### 主链路（节拍串联）

| 步 | 动作 | 来源 Scenario | 期望 | 层 | 落点 |
| --- | --- | --- | --- | --- | --- |
| 1 | 打开 shared/test-design.md 查规范 | 权威源结构齐全 | 单元模型/模板/问句⓪-④/措辞/白盒/JIT 摘要共 13 节 | 文档结构 | 人工：V.1 grep 节标题 |
| 2 | 按 Requirement 设计测试落点 | 故事锚点与对账同锚 | 单元=完整故事；机器对账锚 tasks 验证节，同一批落点不另起炉灶 | 人工 | 人工：V.1 单元模型节表述 |
| 3 | 写 test-cases.md | 用例文档串故事 | 主链路表六列可读出完整用户旅程 | 人工 | 本文档即活例 |
| 4 | 设计前端交互 change 落点 | 前端交互故事 opencli 落点 | 层选择表含 opencli 行 + 「主链路至少一个落点或人工豁免」 | 人工 | 人工：V.1 问句③节 |
| 5 | 扩充摘要节 | 摘要节体积受控 | ≤3072B（实测 2506） | 文档结构 | 人工：V.2 awk wc -c |
| 6 | 查与 testing.md 分工 | 与既有规范的关系 | 只管测什么不复写怎么跑；两份 testing.md 仅新增节 | 文档结构 | 人工：V.4 diff |

### 变体走查

| # | 变体（组/条目） | 期望答案 | 层 | 落点 |
| --- | --- | --- | --- | --- |
| 1 | 输入-超长：摘要持续扩充逼近 3KB | 体积硬约束 + 既有分层降级兜底，不为本文档新增降级规则 | 人工 | spec Scenario「边界——注入预算超线降级」+ V.2 |
| 2 | 时间窗口 / 幂等变体 | 不适用——文档标准无时间窗口/幂等语义 | — | 划除留痕 |

### 效果核对

- 触发原因：标准的效果在于**后续 change 的用例质量**（agent 行为改变），本 change 断言不了
- 核对方法：watch-keyword-and-quickadd 试点（V.7）+ ⑤a 关键词误报率观察（词表收紧为预留退出条件）
- 量化结果：试点后回填；当前仅冒烟 121+14 断言 + watch-keyword 真机命中 3 条（T.2 留痕）
- 结论：规范机制交付完成，效果验证移交试点

## 故事 S2: 编辑 change / 测试文件时自动收到设计规范（锚 Requirement: 设计规范 JIT 注入）

### 主链路（节拍串联）

| 步 | 动作 | 来源 Scenario | 期望 | 层 | 落点 |
| --- | --- | --- | --- | --- | --- |
| 1 | 编辑 openspec/changes/*/specs/*.md | 编辑 specs 时注入 | 下一回合 system prompt 含摘要节 | 冒烟+真机 | .pi/extensions/tests/constraint-injection.smoke.cjs + V.6 events.db |
| 2 | 编辑 backend-go/**/*_test.go | 编辑测试代码时注入 | 同一摘要注入（四信号同一文档） | 冒烟 | 同上（bar_test.go 命中断言） |
| 3 | 仅编辑 docs/research/** | research 语境不注入 | 零注入（信号不含该路径） | 人工 | 人工：标签信号清单核对 |
| 4 | 多域命中致注入逼近 32KB | 边界——注入预算超线降级 | keyword 优先于 jit，摘要首个被降 | 人工 | 人工：既有分层降级机制，V.2 保摘要不主动触线 |

### 变体走查

| # | 变体（组/条目） | 期望答案 | 层 | 落点 |
| --- | --- | --- | --- | --- |
| 1 | 输入-信号重叠：openspec/changes/ 与 _test.go 同会话先后命中 | 文档级 Map 去重，只注一份不叠加 | 冒烟 | smoke JIT 追加/去重断言组 |
| 2 | 前置-冷启动：裸会话首编辑测试文件（未先碰 change 文件） | 一次性前缀失效 ≤1 次/会话，此后粘性 | 人工 | 缓存分析留痕（会话讨论，2026-08-24） |
| 3 | 幂等-同会话重复编辑同路径 | 粘性只增不减，无重复注入 | 冒烟 | smoke JIT 粘性断言 |

## 故事 S3: 归档时收到措辞 warn 但不被阻断（锚 Requirement: 归档措辞 warn 检查）

### 主链路（节拍串联）

| 步 | 动作 | 来源 Scenario | 期望 | 层 | 落点 |
| --- | --- | --- | --- | --- | --- |
| 1 | 干净 tasks.md 归档 | 无违例静默放行 | 0 warning，裁决与现状一致 | 纯函数冒烟 | .pi/extensions/tests/spec-gate.smoke.cjs 断言① |
| 2 | 「解析」任务行 + 无 test-cases*.md | 白盒用例缺失 warn 留痕 | 1 条含「可忽略」，不 block | 冒烟+真机 | smoke 断言② + watch-keyword 实测（T.2 留痕 3 条） |
| 3 | 纯函数+SQLite 同任务行 | 纯函数任务提 SQLite warn 留痕 | 1 条含 *_unit_test.go 指引 | 冒烟+真机 | smoke 断言③ + watch-keyword 1.3/1.4 命中 |
| 4 | tasks.md 读不到 / 扫描异常 | 检查⑤异常不阻断归档 | fail-open 跳过，裁决按①-④ | 冒烟+代码 review | smoke 健壮性断言④ + wrapper try/catch |
| 5 | --force / SPEC_GATE_BYPASS=1 | 豁免通道兼容 | ⑤随豁免短路，零追加 block | 人工 | 人工：代码 review（豁免 return 先于⑤） |

### 变体走查

| # | 变体（组/条目） | 期望答案 | 层 | 落点 |
| --- | --- | --- | --- | --- |
| 1 | 输入-去重：同关键词多行命中 | 按关键词去重每词一条 | 冒烟 | smoke「⑤a 去重：解析×2+状态机×1 → 2 条」 |
| 2 | 输入-半命中：纯函数 / SQLite 各自单独出现 | 0 条（不误伤描述性单提及） | 冒烟 | smoke「⑤b 半命中 → 0 条」 |
| 3 | 前置-文件名近似：test-case.md / testcase.md | 不算检出（精确 glob），照常提醒 | 冒烟 | smoke「近似名不算」 |
| 4 | 前置-目录列表拿不到 | 伪造成已检出抑制⑤a（证明不了缺失就不提醒），⑤b 照跑 | 人工 | warnAcceptanceWording 降级分支（代码 review） |
| 5 | 幂等-纯函数重复调用 | 确定性输出，同输入同结果 | 冒烟 | smoke 多行各一条断言 |

## 故事 S4: 挂接可查（锚 Requirement: 前后端标准挂接与索引）

### 主链路（节拍串联）

| 步 | 动作 | 来源 Scenario | 期望 | 层 | 落点 |
| --- | --- | --- | --- | --- | --- |
| 1 | 写后端测试前查判据 | 后端判据可查 | backend/testing.md 含「用例设计（测什么）」节引用 shared | 文档结构 | 人工：V.4 grep |
| 2 | 未激活档看常驻索引 | 索引可见 | constraints-index 执行规范表含 test-design 行 | 文档结构 | 人工：V.4 grep |
| 3 | 比对既有权威内容 | 既有权威内容不变 | DSN 红线 / 禁 SQLite 原样，diff 仅 +10/+10 | 文档结构 | 人工：V.4 diff |

### 变体走查

不适用——纯挂接无输入/状态语义。划除留痕。

## 故事 S5: 改契约时能反查旧测试资产（锚 Requirement: 回归走查与旧资产可见性）

### 主链路（节拍串联）

| 步 | 动作 | 来源 Scenario | 期望 | 层 | 落点 |
| --- | --- | --- | --- | --- | --- |
| 1 | 对存在 capability 查询 | 反向索引三段输出 | 三段输出 + 退出码 0（实测 11 行映射重建） | 脚本实测 | 人工：T.5 正路径留痕 |
| 2 | 对不存在 capability 查询 | 查询不存在即报错 | 退出码 1 + 中文提示，无空段充数 | 脚本实测 | 人工：T.5 负路径留痕 |
| 3 | 改契约 change 写 test-cases.md | 改契约必答继承表 | 「继承与调整」四列表逐行有处置 | 人工 | 人工：问句⓪节格式（本 change 无改契约场景，规则载体即验收） |
| 4 | 纯新增 change（delta 全 ADDED） | 纯新增豁免 | 无需继承表且不报违例 | 人工 | 本 change 即活例（见下节豁免说明） |
| 5 | 写 specs 时被 JIT 注入 | 摘要含回归提醒 | 摘要含问句⓪句 + test-assets.sh 指引 | 文档结构+冒烟 | 人工：V.8 grep + smoke 摘要断言 |

### 变体走查

| # | 变体（组/条目） | 期望答案 | 层 | 落点 |
| --- | --- | --- | --- | --- |
| 1 | 输入-空参数调用 | usage 提示 + 退出码 1 | 脚本实测 | 验收时实测（见 V.9 留痕） |
| 2 | 前置-archive 无 test-cases*.md | 段2 如实标 [无]，不造假 | 脚本实测 | scenario-trace-gate 查询输出即证（当前全 archive 无） |
| 3 | 前置-主 specs 不存在仅 archive 有 | 段1 提示「已无现行契约，仅存历史」 | 人工 | 脚本分支 review（未构造实例） |
| 4 | 前置-历史 tasks.md 无验证节映射表 | 段3 提示早期格式未留映射，不算错 | 人工 | 脚本分支 review |
| 5 | 幂等-重复执行 | 只读脚本，输出一致 | 人工 | 脚本零写操作（只读只判） |

## 继承与调整（问句⓪）

**豁免**：本 change delta 全为 ADDED Requirements（新增 capability `test-case-design`），无 MODIFIED/REMOVED，纯新增豁免——无旧节拍可继承，不填表。

## 白盒附加（复杂档）

**划除**：本 change 无状态机（≥3 状态循环）/ 算法分支爆炸 / 多模块协议——⑤扫描为行级文本匹配（冒烟 14 断言已覆盖其分支：去重/抑制/半命中/健壮性），test-assets.sh 为 grep/awk 单遍扫描（正/负路径实测覆盖）。复杂档判据不满足。
